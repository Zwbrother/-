- 所有页面组件均按角色维度组织在 `src/pages/{role}/` 目录下，保持功能模块的物理隔离。
- API 响应统一遵循 `{ code, data, message }` 结构，拦截器中针对 `code === 0` 成功态和 `code === 401` 失效态进行标准化处理。
- 布局组件通过配置化的菜单项数组（menuItems）驱动侧边栏渲染，实现布局逻辑与导航内容的解耦。