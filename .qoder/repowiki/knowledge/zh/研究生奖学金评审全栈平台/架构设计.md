- 采用前后端分离架构，前端通过 `frontend/src/api.js` 统一封装 HTTP 请求与后端 RESTful API 通信。
- 后端基于 Spring Boot 提供分层服务（Controller-Service-Mapper），并通过 JWT 拦截器实现跨角色的统一身份认证与权限隔离。
- 根目录下的 `start-backend.ps1` 和 `start-frontend.ps1` 脚本提供了标准化的本地开发环境一键启动流程，解耦了前后端的运行依赖。