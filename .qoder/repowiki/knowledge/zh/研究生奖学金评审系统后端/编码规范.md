- 所有 API 响应均封装为统一的 `R<T>` 对象，包含 code、message 和 data 字段。
- 业务校验失败时统一抛出 `BusinessException`，由全局异常处理器捕获并转换为对应的 HTTP 状态码。
- Controller 层通过构造函数注入 Service 和 Mapper 依赖，保持无状态设计。
- 实体类（Entity）使用 Lombok 的 `@Data` 注解简化代码，并配合 MyBatis-Plus 的 `@TableName` 和 `@TableId` 进行映射。